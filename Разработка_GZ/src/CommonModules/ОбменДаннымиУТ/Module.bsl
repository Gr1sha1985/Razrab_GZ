Процедура ЗаписьРегистраПрефиксыИнформационныхБазПоУмолчанию(МассивПрефиксов, ПечататьПрефикс = Истина, ФорматБСП = Истина) Экспорт
	
	МассивСтруктур = Новый Массив();
	
	Для Каждого ПрефиксИБ Из МассивПрефиксов Цикл
		СтруктураЗаписи = Новый Структура("Префикс, ПечататьПрефикс, ФорматБСП", ПрефиксИБ, ПечататьПрефикс, ФорматБСП);
		МассивСтруктур.Добавить(СтруктураЗаписи);
	КонецЦикла;
	
	РегистрыСведений.ПрефиксыИнформационныхБаз.ЗагрузитьИнформациюОПрефиксах(МассивСтруктур);
	
КонецПроцедуры

Функция ПрефиксИнформационнойБазыИзНомера(НомерОбъекта) Экспорт
	
	Если Не НомерСодержитСтандартныйПрефикс(НомерОбъекта) Тогда
		Возврат "";
	КонецЕсли;
	
	НомерСодержитПятизначныйПрефикс = НомерСодержитПятизначныйПрефикс(НомерОбъекта);
	
	Если НомерСодержитПятизначныйПрефикс Тогда
		ПрефиксОрганизации        = Лев(НомерОбъекта, 2);
		ПрефиксИнформационнойБазы = Сред(НомерОбъекта, 3, 2);
	Иначе
		ПрефиксОрганизации = "";
		ПрефиксИнформационнойБазы = Лев(НомерОбъекта, 2);
	КонецЕсли;
	
	Возврат ПрефиксИнформационнойБазы;
	
КонецФункции

Функция НомерСодержитСтандартныйПрефикс(Знач НомерОбъекта)
	
	ПозицияРазделителя = Найти(НомерОбъекта, "-");
	
	Возврат ПозицияРазделителя = 3
		ИЛИ ПозицияРазделителя = 5
	;
КонецФункции

Функция НомерСодержитПятизначныйПрефикс(Знач НомерОбъекта)
	
	Возврат Найти(НомерОбъекта, "-") = 5;
	
КонецФункции
