
#Область ПрограммныйИнтерфейс

#Область КопированиеВставкаСтрокЧерезБуферОбмена

// Процедура оповещает о копировании строк табличной части
//
// Параметры:
//  Форма            - ФормаКлиентскогоПриложения - форма, откуда произошло копирование строк
//
//  КоличествоСтрок  - Число - количество скопированных строк
//
Процедура ОповеститьОКопированииСтрокВБуферОбмена(Форма, КоличествоСтрок) Экспорт
	
	НавигационнаяСсылка = Форма.Окно.ПолучитьНавигационнуюСсылку();
	ПоказатьОповещениеПользователя(НСтр("ru = 'Копирование в буфер обмена'"), НавигационнаяСсылка,
		СтрШаблон(НСтр("ru = 'Скопировано строк: %1'"), КоличествоСтрок));
	Оповестить("ДанныеСкопированыВБуферОбмена");

КонецПроцедуры

// Процедура оповещает о вставке строк в табличную часть
//
// Параметры:
//  Форма            - ФормаКлиентскогоПриложения - форма, куда была произведена вставка строк
//
//  КоличествоСтрок  - Число - количество вставленных строк
//
Процедура ОповеститьОВставкеСтрокИзБуфераОбмена(Форма, КоличествоСтрок) Экспорт

	НавигационнаяСсылка = Форма.Окно.ПолучитьНавигационнуюСсылку();
	ПоказатьОповещениеПользователя(НСтр("ru = 'Вставка из буфера обмена'"), НавигационнаяСсылка,
		СтрШаблон(НСтр("ru = 'Вставлено строк: %1'"), КоличествоСтрок));

КонецПроцедуры

#КонецОбласти

#КонецОбласти