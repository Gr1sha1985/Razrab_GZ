
// Конструктор параметров для функции РаботаСКурсамиВалютКлиентСервер.ПересчитатьПоКурсу
//
// Параметры:
//  Валюта		 - 	 - 
//  Курс		 - 	 - 
//  Кратность	 - 	 - 
// 
// Возвращаемое значение:
//   - 
//
Функция ПараметрыПересчетаКурса(Валюта, Курс, Кратность) Экспорт
	
	ПараметрыПересчетаКурса = Новый Структура;
	ПараметрыПересчетаКурса.Вставить("Валюта", Валюта);
	ПараметрыПересчетаКурса.Вставить("Курс", Курс);
	ПараметрыПересчетаКурса.Вставить("Кратность", Кратность);
	Возврат ПараметрыПересчетаКурса;
	
КонецФункции

// Устарела. Следует использовать функцию РаботаСКурсамиВалютКлиентСервер.ПересчитатьПоКурсу.
//
// Пересчитывает сумму из валюты ВалютаНач по курсу ПоКурсуНач 
// в валюту ВалютаКон по курсу ПоКурсуКон.
//
// Параметры:
//   Сумма          - Число - Сумма, которую следует пересчитать.
//   ВалютаНач      - СправочникСсылка.Валюты - Валюта, из которой надо пересчитать.
//   ВалютаКон      - СправочникСсылка.Валюты - Валюта, в  которую надо пересчитать.
//   ПоКурсуНач     - Число - Курс, из которого надо пересчитать.
//   ПоКурсуКон     - Число - Курс, в  который  надо пересчитать.
//   ПоКратностьНач - Число - Кратность, из которой надо пересчитать (по умолчанию = 1).
//   ПоКратностьКон - Число - Кратность, в  которую надо пересчитать (по умолчанию = 1).
//
// Возвращаемое значение: 
//   Число - Сумма, пересчитанная в другую валюту.
//
Функция ПересчитатьИзВалютыВВалюту(Сумма, ВалютаНач, ВалютаКон, ПоКурсуНач, ПоКурсуКон, 
	ПоКратностьНач = 1, ПоКратностьКон = 1) Экспорт
	
	ПараметрыТекущегоКурса = РаботаСКурсамиВалютБПКлиентСервер.ПараметрыПересчетаКурса(
		ВалютаНач, ПоКурсуНач, ПоКратностьНач);
	ПараметрыНовогоКурса = РаботаСКурсамиВалютБПКлиентСервер.ПараметрыПересчетаКурса(
		ВалютаКон, ПоКурсуКон, ПоКратностьКон);
	
	Возврат РаботаСКурсамиВалютКлиентСервер.ПересчитатьПоКурсу(Сумма, ПараметрыТекущегоКурса, ПараметрыНовогоКурса);
	
КонецФункции
