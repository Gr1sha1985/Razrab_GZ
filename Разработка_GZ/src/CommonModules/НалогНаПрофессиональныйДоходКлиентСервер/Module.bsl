#Область ПрограммныйИнтерфейс

Функция ДатаНачалаЭксперимента() Экспорт
	
	Возврат Дата(2019, 1, 1);
	
КонецФункции

// Возвращает ставки налога на профессиональный доход, указанные в законе.
//
// Параметры:
//   Период - Дата - дата, на которую необходимо получить значения ставок.
//
// Возвращаемое значение:
//  Структура - ставки налогов.
//    * ДоходыОтФизЛиц - Число.
//    * ДоходыОтЮрЛицИП - Число.
//
Функция НалоговыеСтавки(Период) Экспорт
	
	Результат = Новый Структура;
	Результат.Вставить("ДоходыОтФизЛиц", 4);
	Результат.Вставить("ДоходыОтЮрЛицИП", 6);
	
	Возврат Результат;
	
КонецФункции

// Возвращает предел доходов налогоплательщика, ограничивающий его право на применение спецрежима "Налог на профессиональный доход"
//
// Возвращаемое значение:
//  Число - Число - Граница доходов, ограничивающая право применения НПД
//
Функция ПределДоходов(Период) Экспорт
	
	// Лимит доходов налогоплательщика, ограничивающий его право на применение НПД
	Возврат 2400000;
	
КонецФункции

// Возвращает ставки налогового вычета и максимальную сумму вычета, указанные в законе.
//
// Параметры:
//   Период - Дата - дата, на которую необходимо получить ставки вычета и предел.
//
// Возвращаемое значение:
//   Структура - структура с ключами:
//      * СтавкаДоходыОтФизЛиц - Число.
//      * СтавкаДоходыОтЮрЛицИП - Число.
//      * Предел - Число.
//
Функция ПараметрыНалоговогоВычета(Период) Экспорт
	
	Результат = Новый Структура;
	Результат.Вставить("СтавкаДоходыОтФизЛиц", 1);
	Результат.Вставить("СтавкаДоходыОтЮрЛицИП", 2);
	Результат.Вставить("Предел", 10000);
	
	Возврат Результат;
	
КонецФункции

#КонецОбласти

#Область СлужебныйПрограммныйИнтерфейс
// Код процедур и функций
#КонецОбласти

#Область СлужебныеПроцедурыИФункции
// Код процедур и функций
#КонецОбласти