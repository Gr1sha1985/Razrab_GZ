
&НаКлиенте
Процедура ИмяФайлаОткрытие(Элемент, СтандартнаяОбработка)
	СтандартнаяОбработка=Ложь;
	
	КаталогПоиска  = ГЗ_РаботаСXML.ВернутьПутьХраненияФайлов("Договор44ФЗ");	
	
    ЗапуститьПриложение(КаталогПоиска+объект.ИмяФайла);

КонецПроцедуры

&НаКлиенте
Процедура url_ПодписанногоДоговораОткрытие(Элемент, СтандартнаяОбработка)
	СтандартнаяОбработка=Ложь;
	ЗапуститьПриложение(объект.url_ПодписанногоДоговора);
КонецПроцедуры

	