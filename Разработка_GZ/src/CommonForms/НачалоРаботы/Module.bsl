#Область ОбработчикиСобытийФормы

&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	
	ДоступностьОрганизаций = ОбщегоНазначенияБПВызовСервера.ПроверитьНаличиеДоступныхОрганизаций();
	
	Элементы.ГруппаНачалоРаботы.Видимость = ДоступностьОрганизаций;
	Элементы.НадписьДоступКОрганизациям.Видимость = НЕ ДоступностьОрганизаций;
	
КонецПроцедуры

&НаКлиенте
Процедура ОбработкаОповещения(ИмяСобытия, Параметр, Источник)
	
	// Перечень событий см. ОбщиеФормы.СинхронизацияДанных.ОбработкаОповещения()
	СобытияСинхронизацииДанных = Новый Массив;
	СобытияСинхронизацииДанных.Добавить("ВыполненОбменДанными");
	СобытияСинхронизацииДанных.Добавить("Запись_СценарииОбменовДанными");
	СобытияСинхронизацииДанных.Добавить("Запись_УзелПланаОбмена");
	СобытияСинхронизацииДанных.Добавить("ЗакрытаФормаПомощникаСопоставленияОбъектов");
	СобытияСинхронизацииДанных.Добавить("ЗакрытаФормаПомощникаСозданияОбменаДанными");
	СобытияСинхронизацииДанных.Добавить("ЗакрытаФормаРезультатовОбменаДанными");
	
	Если СобытияСинхронизацииДанных.Найти(ИмяСобытия) <> Неопределено И НужноОбновитьИнтерфейс() Тогда
		ОбновитьИнтерфейс();
	КонецЕсли;
	
КонецПроцедуры

#КонецОбласти

#Область ОбработчикиКомандФормы

&НаКлиенте
Процедура ЗаполнитьРеквизиты(Команда)
	
	ОткрытьФорму("Справочник.Организации.ФормаОбъекта");
	
КонецПроцедуры

&НаКлиенте
Процедура ВидеоурокЗаполнениеРеквизитов(Команда)
	
	НачалоРаботыКлиент.ВидеоурокЗаполнениеРеквизитов();
	
КонецПроцедуры

&НаСервереБезКонтекста
Функция НужноОбновитьИнтерфейс()
	
	Возврат НЕ Константы.НачалоРаботы.Получить();
	
КонецФункции

#КонецОбласти